{
  "name": "buildlab-whatsapp-render-bot",
  "version": "1.0.0",
  "private": true,
  "description": "BuildLab Zambia WhatsApp Cloud API webhook and automatic reply bot for Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
